{
  "name": "restic-browser",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot serve.ts",
    "build": "bun build src/app.ts --outdir=dist --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^2.0.1",
    "aws4fetch": "^1.0.20",
    "fzstd": "^0.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "bun-types": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
